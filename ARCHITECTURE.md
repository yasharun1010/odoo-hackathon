# System Architecture Diagrams

## 1. High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:3000                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/REST
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    REACT FRONTEND (Vite)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Pages      │  │  Components  │  │   Context    │          │
│  │              │  │              │  │              │          │
│  │ - Login      │  │ - Button     │  │ - Auth       │          │
│  │ - Dashboard  │  │ - Input      │  │              │          │
│  │ - Expenses   │  │ - Modal      │  │              │          │
│  │ - Approvals  │  │ - Badge      │  │              │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────┐           │
│  │         API Service Layer (Axios)                │           │
│  │  - JWT Token Management                          │           │
│  │  - Request/Response Interceptors                 │           │
│  │  - Error Handling                                │           │
│  └──────────────────────────────────────────────────┘           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ REST API (JSON)
                             │ Port 5000
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                    FLASK BACKEND                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │   Routes     │  │   Services   │  │    Models    │          │
│  │              │  │              │  │              │          │
│  │ /api/auth/*  │  │ - Currency   │  │ - User       │          │
│  │ /api/users/* │  │ - OCR        │  │ - Company    │          │
│  │ /api/exp/*   │  │ - Approval   │  │ - Expense    │          │
│  │ /api/appr/*  │  │              │  │ - ApprovalRule│         │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│                                                                  │
│  ┌──────────────────────────────────────────────────┐           │
│  │         Authentication & Authorization           │           │
│  │  - JWT Validation                                │           │
│  │  - Role-Based Access Control                     │           │
│  │  - Password Hashing (Bcrypt)                     │           │
│  └──────────────────────────────────────────────────┘           │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ SQLAlchemy ORM
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      SQLite DATABASE                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────       │
│  │  Users   │  │ Company  │  │ Expenses │  │ Approvals│       │
│  │          │  │          │  │          │  │          │       │
│  │ - id     │  │ - id     │  │ - id     │  │ - id     │       │
│  │ - email  │  │ - name   │  │ - amount │  │ - step   │       │
│  │ - role   │  │ - currency│ │ - status │  │ - status │       │
│  └──────────┘  └──────────  └──────────┘  └──────────┘       │
│                                                                  │
│  ┌──────────────────┐                                          │
│  │  ApprovalRules   │                                          │
│  │                  │                                          │
│  │ - rule_type      │                                          │
│  │ - percentage     │                                          │
│  └──────────────────┘                                          │
└──────────────────────────────────────────────────────────────────┘
```

---

## 2. Authentication Flow

```
┌─────────┐                              ┌─────────┐
│  User   │                              │ Backend │
└────┬────┘                              └────┬────┘
     │                                        │
     │  1. POST /api/auth/login               │
     │  {email, password}                     │
     ├───────────────────────────────────────>│
     │                                        │
     │                                        │ 2. Verify credentials
     │                                        │    Check password hash
     │                                        │
     │  3. Return JWT tokens                  │
     │  {access_token, refresh_token, user}   │
     │<───────────────────────────────────────┤
     │                                        │
     │  4. Store tokens in localStorage       │
     │                                        │
     │                                        │
     │  Every subsequent request:             │
     │  Header: Authorization: Bearer <token> │
     ├───────────────────────────────────────>│
     │                                        │
     │                                        │ 5. Validate JWT
     │                                        │    Check signature
     │                                        │    Check expiry
     │                                        │
     │  6. Process request                    │
     │<───────────────────────────────────────┤
     │                                        │
```

---

## 3. Expense Approval Workflow

```
┌──────────────┐
│   Employee   │
└──────┬───────┘
       │
       │ 1. Submit Expense
       │    (Amount: $500, Category: Travel)
       ▼
┌─────────────────────────────────┐
│      Expense Created            │
│      Status: PENDING            │
└──────┬──────────────────────────┘
       │
       │ 2. Determine Approval Chain
       │    (Based on hierarchy)
       ▼
┌──────────────┐
│   Manager 1  │◄─── Step 1: Pending Approval
└──────┬───────┘
       │
       │ 3. Approve
       ▼
┌──────────────┐
│   Manager 2  │◄─── Step 2: Pending Approval
└──────┬───────┘
       │
       │ 4. Approve
       ▼
┌──────────────┐
│   Director   │◄─── Step 3: Pending Approval
└──────┬───────┘
       │
       │ 5. Final Approval
       ▼
┌─────────────────────────────────┐
│   Expense Approved              │
│   Status: APPROVED ✓            │
│   Ready for reimbursement       │
└─────────────────────────────────┘
```

---

## 4. Approval Rules Engine

```
┌─────────────────────────────────────────┐
│      New Expense Submitted              │
│      Category: {category}               │
│      Amount: ${amount}                  │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│   Get Applicable Rule                   │
│   (Category-specific or Default)        │
└───────────────┬─────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
┌─────────────┐  ┌─────────────┐
│ PERCENTAGE  │  │  SPECIFIC   │
│   RULE      │  │  APPROVER   │
│             │  │    RULE     │
│ Requires:   │  │             │
│ 60% approval│  │ Requires:   │
│             │  │ CFO approval│
└──────┬──────┘  └──────┬──────┘
       │                │
       └────────┬───────┘
                │
                ▼
┌─────────────────────────────────────────┐
│         HYBRID RULE                     │
│                                         │
│  (60% approval OR CFO approval)         │
│                                         │
│  ✓ If 60% approve → Auto-approve        │
│  ✓ If CFO approves → Auto-approve       │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│   Check: Sequential or Parallel?        │
│                                         │
│  Sequential: One after another          │
│  Parallel: All at once                  │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│   Create Approval Records               │
│   Status: PENDING                       │
└─────────────────────────────────────────┘
```

---

## 5. Currency Conversion Flow

```
┌──────────────────┐
│  Employee in UK  │
│  Base Currency:  │
│  GBP (£)         │
└────────┬─────────┘
         │
         │ Submits expense in EUR
         │ Amount: €100
         ▼
┌─────────────────────────────────┐
│   Expense Service               │
│   Receives:                     │
│   - Amount: 100                 │
│   - Currency: EUR               │
└────────────────────────────────┘
         │
         │ Calls Currency Service
         ▼
┌─────────────────────────────────┐
│   Currency Service              │
│   1. Check API Key configured?  │
│      Yes → Call ExchangeRate API│
│      No → Use Mock Rates        │
│                                 │
│   2. Get Rate: EUR → GBP       │
│      Rate: 0.85                 │
│                                 │
│   3. Calculate:                 │
│      £100 × 0.85 = £85          │
└────────┬────────────────────────┘
         │
         │ Returns:
         │ - Original: €100
         │ - Converted: £85
         │ - Rate: 0.85
         ▼
┌─────────────────────────────────┐
│   Store in Database             │
│   - amount: 100                 │
│   - currency: EUR               │
│   - converted_amount: 85        │
│   - company_currency: GBP       │
└─────────────────────────────────┘
```

---

## 6. Receipt OCR Processing

```
┌──────────────────┐
│   User Uploads   │
│   Receipt Image  │
│   or PDF         │
└────────┬─────────┘
         │
         │ File Upload
         │ (multipart/form-data)
         ▼
┌─────────────────────────────────┐
│   Backend Receives File         │
│   1. Validate file type         │
│      (PDF, PNG, JPG, JPEG)      │
│   2. Check size (< 16MB)        │
│   3. Save to uploads/receipts/  │
└────────┬────────────────────────┘
         │
         │ Call OCR Service
         ▼
┌─────────────────────────────────┐
│   OCR Service                   │
│                                 │
│   If Image (PNG/JPG):           │
│   - Pytesseract OCR             │
│   - Extract text                │
│                                 │
│   If PDF:                       │
│   - PyPDF2 text extraction      │
│   - Parse text                  │
│                                 │
│   Pattern Matching:             │
│   - Amount: \$\d+\.\d{2}        │
│   - Date: \d{4}-\d{2}-\d{2}     │
│   - Merchant: First line        │
└────────┬────────────────────────┘
         │
         │ Returns extracted data:
         │ {
         │   amount: 100.50,
         │   date: "2024-01-15",
         │   merchant: "Restaurant"
         │ }
         ▼
┌─────────────────────────────────┐
│   Pre-fill Expense Form         │
│   with OCR data                 │
│                                 │
│   User can review and edit      │
└─────────────────────────────────┘
```

---

## 7. Database Entity Relationship

```
┌─────────────────┐
│    COMPANY      │
│─────────────────│
│ PK id           │
│    name         │
│    currency     │
│    country      │
└────────┬────────┘
         │ 1:N
         │
         ▼
┌─────────────────┐         ┌─────────────────┐
│     USER        │         │   EXPENSE       │
│─────────────────│         │─────────────────│
│ PK id           │◄────────│ PK id           │
│ FK company_id   │         │ FK user_id      │
│ FK manager_id   │  1:N    │ FK company_id   │
│    email        │────────>│    amount       │
│    password     │         │    currency     │
│    role         │         │    status       │
│    is_active    │         │    category     │
└────────┬────────┘         └────────────────┘
         │                           │ 1:N
         │                           │
         │                           ▼
         │                  ┌─────────────────┐
         │                  │   APPROVAL      │
         │                  │─────────────────│
         │                  │ PK id           │
         │◄─────────────────│ FK expense_id   │
         │  N:M             │ FK approver_id  │
         │                  │    step         │
         │                  │    status       │
         │                  │    comment      │
         │                  └─────────────────┘
         │
         │ N:M
         │
         ▼
┌─────────────────┐
│ APPROVAL_RULE   │
│─────────────────│
│ PK id           │
│ FK company_id   │
│    rule_type    │
│    percentage   │
│    is_active    │
└─────────────────┘
```

---

## 8. Component Hierarchy

```
App.jsx
│
├── BrowserRouter
│
├── AuthProvider
│   │
│   └── Routes
│       │
│       ├── Public Routes
│       │   ├── Login
│       │   └── Signup
│       │
│       └── Protected Routes
│           │
│           ├── Dashboard
│           │   └── Layout
│           │       ├── Navigation
│           │       └── Content
│           │
│           ├── Expenses
│           │   └── Layout
│           │       ├── ExpenseList
│           │       │   └── Table
│           │       │       └── Badge
│           │       └── Button
│           │
│           ├── SubmitExpense
│           │   └── Layout
│           │       ├── Form
│           │       │   ├── Input
│           │       │   ├── Select
│           │       │   └── Button
│           │       └── Modal
│           │
│           ├── ApprovalsDashboard (Manager/Admin)
│           │   └── Layout
│           │       ├── ApprovalTable
│           │       │   └── Badge
│           │       └── Modal
│           │           ├── Form
│           │           └── Button
│           │
│           ├── UserManagement (Admin)
│           │   └── Layout
│           │       ├── UserTable
│           │       └── Modal
│           │           └── Form
│           │
│           └── ApprovalRules (Admin)
│               └── Layout
│                   ├── RulesTable
│                   └── Modal
│                       └── Form
```

---

## 9. Data Flow Example

```
User Action → Frontend → API → Backend → Database

Example: Submit Expense

1. User fills form and clicks "Submit"
   ↓
2. React component validates form
   ↓
3. Axios creates FormData with file
   ↓
4. POST /api/expenses/ with JWT token
   ↓
5. Flask receives request
   ↓
6. JWT decorator validates token
   ↓
7. Expense route handler processes
   ↓
8. Save file to uploads/receipts/
   ↓
9. Call OCR service for extraction
   ↓
10. Call Currency service for conversion
    ↓
11. Create Expense model instance
    ↓
12. Call Approval Workflow service
    ↓
13. Create Approval records
    ↓
14. Commit to database
    ↓
15. Return JSON response
    ↓
16. Frontend shows success toast
    ↓
17. Navigate to expense list
```

---

## 10. Security Layers

```
┌─────────────────────────────────────────┐
│          Client Side                    │
│  - Input Validation                     │
│  - Form Validation                      │
│  - Token Storage (localStorage)         │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│          Network Layer                  │
│  - HTTPS (Production)                   │
│  - CORS Configuration                   │
│  - JWT in Headers                       │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│          Application Layer              │
│  - JWT Authentication                   │
│  - Role-Based Access Control            │
│  - Input Sanitization                   │
│  - SQL Injection Prevention (ORM)       │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│          Data Layer                     │
│  - Password Hashing (Bcrypt)            │
│  - Parameterized Queries                │
│  - File Upload Validation               │
└─────────────────────────────────────────┘
```

---

**These diagrams illustrate the complete system architecture and data flow.**
